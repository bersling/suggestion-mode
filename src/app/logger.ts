export function logger(str: any) {
  // const loggerOutput = document.getElementById('log-output');
  // const currentInnerText = loggerOutput.innerText;
  // loggerOutput.innerText = `${str}\n\n${currentInnerText}`;
}